<!-- source-page: 305 -->
<style>
table{border-collapse:collapse;margin:.6em 0;display:table!important;width:100%!important;max-width:960px!important}
td,th{border:1px solid #bbb;padding:2px 7px;text-align:center;vertical-align:top}
table.bitfield{table-layout:fixed}
table.bitfield td,table.bitfield th{word-break:break-word;font-size:.8em;padding:2px}
</style>
<sub>[← p.304](0304.md) · [index](../README.md) · [PDF p.305](https://ch32-riscv-ug.github.io/CH32V407/datasheet_en/CH32V407RM.PDF#page=305) · [p.306 →](0306.md)</sub>

<!-- header: CH32V407 Reference Manual https://wch-ic.com -->
the ACK/NACK data phase of traditional I2C write transfers, the transition bit of I3C read transfers, or  
the ACK/NACK phase of traditional I2C write transfers (tSTALL). Peripherals acting as master devices  
may adjust the SCL clock low-level stall time and tNEWCRLock according to their requirements to  
accommodate the master role transition process (following the GETACCCR CCC command).  
3) Configure the R32_I3C_TIMINGR2 register to set the SCL clock low-level dwell time. Each phase can  
be individually enabled or disabled to adjust the SCL clock low-level dwell time (to meet the  
requirements of the addressed I3C slave device or traditional I2C slave device during SDA switching).  
● Configure the DA[6:0] bits of the R32_I3C_DEVR0 register to set the device&#x27;s dynamic address  
● Configure the R32_I3C_DEVRx register to set management parameters for any slave device x (x =  
1/2/3/4)  
● Configure the R32_I3C_CFGR register to set the execution mode for frame transfer or target request  
transfer  
● Configure the R32_I3C_INTENR register to set interrupt generation or polling mode for any event  
● After initialization completes, set EN to 1 to enable the I3C peripheral  
<strong>Start the frame transmission initiated by the master device</strong>  
When the master device is in the enabled state (the EN bit in R32_I3C_CFGR register is 1), the software can  
start frame transmission by any of the following configuration methods:  
1. Software trigger: set the TSFSET bit of R32_I3C_CFGR register to 1 by performing a write operation;  
- This operation will cause the hardware to set the CFNFF flag in the R32_I3C_EVR register to request
writing the first control word to the R32_I3C_CTLR register.  
2. No trigger: directly write the first control word to R32_I3C_CTLR register through software.  
Regardless of the initialization method used, the I3C peripheral subsequently transitions to operational status.  
When the control word is not the final message in an I3C frame (i.e., the MEND bit in the R32_I3C_CTLR  
register is not set) and no transmission errors have occurred (i.e., the ERRF bit in the R32_I3C_EVR register  
is not set), the hardware will continue to request the next control word and proceed with frame transmission:  
1) If the C-FIFO is in non-DMA mode (CDMAEN bit in the R32_I3C_CFGR register is 0), the software will  
write the next control word after the CFNFF flag in the R32_I3C_EVR register is set or the corresponding  
interrupt is triggered (CFNFIE bit in the R32_I3C_INTENR register is 1);  
2) If the C-FIFO is in DMA mode (CDMAEN bit in the R32_I3C_CFGR register is set to 1), the corresponding  
I3C DMA request will be automatically processed by the assigned DMA channel, and the next control word  
will be written.  
<strong>Initiate and receive transmissions initiated from devices</strong>  
When the master device is in the enabled state (the EN bit in the R32_I3C_CFGR register is 1), the master  
device can initiate the transmission, and the slave device can also initiate the transmission by issuing a start  
request (driving SDA to a low level), but only if the master device has allowed the hot join request, IBI request  
or master role request through the R32_I3C_DEVR0 register.  
<strong>Perform frame transmission initiated by the master device</strong>  
On the bus, the master device continuously executes frame transfers until the final message is transmitted  
(R32_I3C_EVR register FCF bit is 1) or an error occurs during transmission (R32_I3C_EVR register ERRF  
bit is 1), triggering the corresponding interrupt (if enabled). This process relies on the R32_I3C_CTLR and  
R32_I3C_TDW/TDR registers (written explicitly by software or automatically pushed by assigned DMA  
channels) and the R32_I3C_RDW/RDR registers (read explicitly by software or by assigned DMA channels).  
<!-- image: p305-draw-image-00001 bbox=[499.92, 794.16, 519.84, 804.96] -->
<!-- footer: V1.2 303 -->
